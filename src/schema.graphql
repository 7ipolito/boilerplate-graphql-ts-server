
type User {
  id: ID!
  email: String!
  password: String!
}

type Query {
users: [User!]!
}

type Mutation {
  register(email: String!, password: String!): Boolean!
}

schema {
  query: Query
  mutation: Mutation
}